{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "pnpm -r --parallel --filter='./packages/*'  dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
